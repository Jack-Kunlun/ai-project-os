# AI Project OS 未发布候选状态

状态：`V5.1.1_RELEASE_CANDIDATE`

已发布基线：`v5.1.0`

当前候选：`v5.1.1`。候选从 `v5.1.0` 线性增加源码覆盖率、关键页面 WCAG、生产资源体积预算和隔离本地容器交付门禁，不增加数据库迁移或产品权限。候选范围与验收证据记录在 [V5.1.1 候选说明](v5.1.1.md)。

候选只有在精确提交通过完整 GitHub Actions 工作流、版本一致性和本地容器候选门禁后才能创建标签。只有源码、健康接口或容器启动均不能单独构成发布依据。

GitHub 托管 Runner 的运行状态与本地发布验收分开记录。真实外部模型、Git、OIDC 和第三方 MCP 服务仍需要独立的现场凭据与验收记录；没有配置时必须明确标记为“未现场验证”，不得用本地替代服务结果冒充。
