# AI Project OS 未发布候选状态

状态：`BLOCKED`

已发布基线：`v5.0.0`

候选起始基线：`main@6e4ffb6`

当前源码包含尚未进入 V5.0.0 运行环境的来源证明与 MCP 审计加固。该候选在完成下列门禁前保持冻结，不得创建版本标签、发布镜像或升级本地正式 Compose 环境：

- [x] 迁移目录完整性检查通过，只包含具有 `migration.sql` 的有效迁移。
- [x] 第 49 个迁移后的 PostgreSQL fixture 与 integrity smoke 全部通过。
- [x] 可弃置 PostgreSQL 中的完整迁移链和全部显式数据库门禁通过。
- [x] 从 V5.0.0 同窗口数据库、密钥卷和上传卷备份完成隔离恢复与升级演练，业务计数和迁移记录一致。
- [ ] 新版本号、CHANGELOG、发布说明、镜像版本和数据库迁移记录相互一致。
- [ ] Prisma 生成与校验、类型检查、Lint、完整测试、生产构建、Compose 状态和认证后页面 smoke 均有新鲜证据。

## 已完成的数据库门禁

2026-08-31 在仅绑定 `127.0.0.1:56432` 的可弃置 PostgreSQL 中执行统一数据库门禁：

- `pnpm test:postgres-gates` 覆盖仓库内全部 22 个显式 PostgreSQL gate，结果 `22/22` 通过；每个 gate 使用独立重建的白名单测试数据库。
- V5.0.0 正式本地 app 与 worker 短暂停写后，在同一窗口取得 PostgreSQL custom-format 备份、密钥卷副本和上传卷副本；备份完成后 app 恢复 `healthy`、worker 恢复 `running`。
- 隔离恢复后的迁移账本为 48 条，关键计数为项目 1、来源 1、项目项 1、修订 1、用户 1、工作区 1，其余所核对表为 0，与备份前一致。
- 隔离恢复目标成功应用第 49 个迁移；升级后关键计数保持一致，来源内容哈希不一致数为 0，伪造哈希更新被数据库保护拒绝。
- 密钥卷与上传卷在备份时均无普通文件；分别恢复到一次性卷后仍为 0，确认空卷状态可还原。临时备份只保留至本次本地升级结束，随后精确清理。

真实外部模型、Git、OIDC 和第三方 MCP 服务需要独立的现场凭据与验收记录；没有配置时必须明确标记为“未现场验证”，不得用本地替代服务结果冒充。

解除冻结必须通过一次独立提交更新本文件及对应版本发布说明，并附上实际执行的验证记录。仅有代码变更、健康接口可见或容器启动不构成解冻依据。
