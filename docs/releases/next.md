# AI Project OS 未发布候选状态

状态：`V5.1.2_RELEASE_CANDIDATE`

已发布基线：`v5.1.1`

当前候选：`v5.1.2`。候选从 `v5.1.1` 线性增加生产 Compose 的安全 Cookie 环境变量传递，确保部署层设置 `AI_PROJECT_OS_SECURE_COOKIES=true` 时应用实际收到该配置；不增加数据库迁移或产品权限。候选范围与验收边界记录在 [V5.1.2 候选说明](v5.1.2.md)。

精确候选提交必须通过完整本地发布门禁和 GitHub Actions 后才能创建标签；生产部署还需单独核验环境变量、数据库备份、迁移、健康、重启持久性与 HTTPS Cookie。只有源码、健康接口或容器启动均不能单独构成发布依据。

GitHub 托管 Runner 的运行状态与本地发布验收分开记录。真实外部模型、Git、OIDC 和第三方 MCP 服务仍需要独立的现场凭据与验收记录；没有配置时必须明确标记为“未现场验证”，不得用本地替代服务结果冒充。
