# AI Project OS 未发布候选状态

状态：`NONE`

已发布基线：`v5.1.2`

当前没有已冻结的未发布候选。V5.1.2 的安全 Cookie 配置传递、本地 `releaseEligible:true` 容器门禁和 GitHub Actions CI #13 验收记录已经归档到 [V5.1.2 发布说明](v5.1.2.md)。

后续未发布变更必须重新记录目标版本、起始基线、范围和发布门禁；生产部署还需单独核验环境变量、数据库备份、迁移、健康、重启持久性与 HTTPS Cookie。只有源码、健康接口或容器启动均不能单独构成发布依据。

GitHub 托管 Runner 的运行状态与本地发布验收分开记录。真实外部模型、Git、OIDC 和第三方 MCP 服务仍需要独立的现场凭据与验收记录；没有配置时必须明确标记为“未现场验证”，不得用本地替代服务结果冒充。
