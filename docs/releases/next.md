# AI Project OS 未发布候选状态

状态：`BLOCKED`

已发布基线：`v5.0.1`

目标版本：`v5.1.0`（未发布）

当前候选正在补齐部署安全响应、Worker 健康监控、外部服务现场验收门禁，以及持续集成与最小浏览器回归。V5.0.1 的候选门禁、数据库恢复升级、构建、部署和页面验收记录已经归档到 [V5.0.1 发布说明](v5.0.1.md)。

此候选尚未完成完整 PostgreSQL、容器构建、一次性数据库升级、Worker 健康和浏览器验收，因此不得发布或部署到正式实例。只有源码、健康接口或容器启动均不能单独构成发布依据。

真实外部模型、Git、OIDC 和第三方 MCP 服务已有只读验收门禁，但当前部署没有现场凭据与成功工作流证据，状态仍为“未现场验证”。不得用本地替代服务结果冒充；只有按现场手册操作后 `pnpm external:acceptance` 返回 `ok: true`，此门禁才可关闭。
