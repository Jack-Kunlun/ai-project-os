# AI Project OS 内部开发状态

状态：`INTERNAL_DEVELOPMENT`

正式发布基线：无

当前内部开发基线：`0.1.0-dev.1`

首个正式公开版本：计划 `1.0.0`

当前不记录正式发布候选。V3–V5 的安全、功能和验收材料均为内部研发里程碑记录（非正式发布），分别保留在本目录中；当前版本的容器门禁只能作为内部开发验收，不构成正式发布依据。

后续内部开发变更必须记录范围、验收证据和风险；首个正式 `v1.0.0` 发布前，生产部署入口保持静态禁用。生产部署还需单独核验环境变量、数据库备份、迁移、健康、重启持久性与 HTTPS Cookie。只有源码、健康接口或容器启动均不能单独构成正式发布依据。

GitHub 托管 Runner 的运行状态与本地发布验收分开记录。真实外部模型、Git、OIDC 和第三方 MCP 服务仍需要独立的现场凭据与验收记录；没有配置时必须明确标记为“未现场验证”，不得用本地替代服务结果冒充。
