# AI Project OS 受控生产预发布状态

状态：`CONTROLLED_PRERELEASE`

当前发布候选：`v0.6.0-dev.14`（预发布，非稳定版；实际生产版本以公网 `/api/health` 为准）

最近完成的受控升级：`v0.6.0-dev.10`→`v0.6.0-dev.11`（116→117 条迁移，停写异地备份与现场验收通过）

最近确认的生产运行基线：`v0.6.0-dev.13`（2026-09-25 已由公网健康接口确认）

首个正式公开版本：计划 `1.0.0`

v0.6.0-dev.10 是此次迁移的历史生产源基线；v0.6.0-dev.11 已发布本次用户反馈与个人通用记忆改动。这些版本都不是稳定版或 GitHub Latest。

## v0.6.0-dev.14 受控发布候选

计划从 `.13` 通过无迁移的 **Deploy application** 通道发布。Token 活动热图的日期格将视觉间隙纳入命中区域，移入后立即显示数值提示；键盘聚焦热图后可使用方向键逐日或逐周查看。Git 等连接表单的帮助图标缩小，长提示浮于弹窗内容区之上并可滚动阅读。完整候选、主分支与标签 CI、停写备份及生产验收以实际运行结果为准。

## v0.6.0-dev.13 受控发布候选

已从 `.12` 通过无迁移的 **Deploy application** 通道发布。额度页 Token 活动改用较亮的蓝紫渐变，年度热图随卡片宽度铺开，短周期热图居中；个人工作台的用户可见名称统一为“我的空间”，入口路径与权限边界不变。主分支与标签 CI、停写备份、生产健康及认证后页面验收均通过。发布证据：[主分支 CI](https://github.com/Jack-Kunlun/ai-project-os/actions/runs/36089704757)、[精确标签 CI](https://github.com/Jack-Kunlun/ai-project-os/actions/runs/36091873194)、[生产部署](https://github.com/Jack-Kunlun/ai-project-os/actions/runs/36092026625)。

## v0.6.0-dev.12 受控生产预发布

从 `.11` 通过无迁移的 **Deploy application** 通道发布。项目导航按使用频率调整，成员页回退到上一页并缩短标签；个人工作区 Owner 可访问自己的团队管理；额度页加入年度 Token 活动热图，账单与其他表格内容垂直居中。主分支与标签 CI、停写异地备份和公网 app/数据库/worker 健康检查通过；认证后的 PetCare 权限与页面视觉复核仍需浏览器验收。

发布证据：[主分支 CI](https://github.com/Jack-Kunlun/ai-project-os/actions/runs/36024522698)、[精确标签 CI](https://github.com/Jack-Kunlun/ai-project-os/actions/runs/36028402045)、[生产部署](https://github.com/Jack-Kunlun/ai-project-os/actions/runs/36029289350)。生产应用提交为 `d98d0e990fa842cecdaf89b7f23e9dc1fa4e522a`。

## v0.6.0-dev.11 受控生产预发布

状态：候选、主分支与精确标签 CI 已通过；停写异地备份返回 `source_quiesced=true`，生产迁移后预检确认 117 条账本，公网 app、数据库与 worker 健康。个人工作台纳入项目入口与项目成员管理；个人知识支持图片和扫描 PDF 提取、混合 PDF 的可选逐页视觉识别、图谱审核、显式标记的个人通用记忆；Git/MCP 连接允许重新测试后修改地址与认证。项目 AI 仅装配当前发起者标记的个人默认记忆；项目约定优先，生成结果可由项目成员共享。此次数据库迁移已通过 **Deploy v0.6 default-memory migration** 完成。

发布证据：[主分支 CI](https://github.com/Jack-Kunlun/ai-project-os/actions/runs/35994822028)、[精确标签 CI](https://github.com/Jack-Kunlun/ai-project-os/actions/runs/35994866598)、[生产部署](https://github.com/Jack-Kunlun/ai-project-os/actions/runs/35998880420)。生产应用提交为 `09670ec70250a240a03897883e019f2e81c79956`。

新增知识图谱和连接编辑页面后，生产构建的全部静态客户端资源实测为 720.8 KiB gzip；总量预算从 720 KiB 调至 724 KiB（约增加 0.56%），共享脚本、单文件、全局 CSS 和关键路由预算不变。

## v0.6.0-dev.10 发布候选

状态：此次 `.11` 迁移的历史生产源基线。继承 `.9` 的产品改动，并修复现有 `.8` 服务器更新器对备份脚本历史版本字面量的兼容校验；`.9` 标签保持不可变。相对 `.8` 不改 Prisma schema、迁移或数据库配置。

## v0.6.0-dev.9 发布候选

状态：候选分支、主分支和精确标签 CI 均通过；生产工作流在发布工具引导阶段因 `RELEASE_TOOLING_BACKUP_CONTRACT_INVALID` 失败，未执行应用切换。候选相对 `.8` 不改 Prisma schema、迁移或数据库配置；发布仍要求停写、异地备份、116 条迁移账本预检和 app/worker 健康检查。

- 用量页增加周期摘要、居中账单表和可选项目筛选；团队邀请与创建账号入口恢复可见。
- 个人工作台包含项目导航，跨项目搜索优先展示项目结果；个人知识支持 TXT、Markdown、JSON、PDF、DOCX 的提取预览，核对后手动保存。
- Git/MCP 新增连接改为浅色弹窗和列表管理；提示图标与字段标签相邻，项目概览操作链接底部对齐。
- 图片、扫描 PDF 提取，已配置 Git/MCP 连接的完整编辑，以及问答层项目记忆优先级仍未完成，本候选不得宣称这些能力已上线。

## v0.6.0-dev.8 发布候选

状态：针对已运行 `v0.6.0-dev.7` 的受控迁移；要求 107 条源账本、stopped-writer 备份、116 条目标账本和新增数据库对象全部通过。

- Actions 只允许从精确 `.7` 源标签部署到 `.8`，先更新 root-owned 发布工具，再执行停写备份、9 条迁移、权限 reconcile、app/worker 切换和公网健康检查。
- 产品交付包括四个一级入口、个人工作台三级结构、独立额度页、项目单入口、Dashboard 个人板块、连接 tooltip 示例，以及 Git/MCP 测试成功后才可保存。
- 补丁路径不启动 `principal-bootstrap`、`migrate` 或 `reconcile`，发现迁移目录差异、账本变化、writer 未停止或健康检查失败即失败关闭。
- `.6` 的 0.5→0.6 历史迁移路径继续保留用于审计与恢复，`.5` 及更早候选标签保持不可移动且不可复用。

## v0.6.0-dev.6 发布候选

状态：已完成产品与迁移代码的精确标签 CI、迁移和生产切换，现作为 `v0.6.0-dev.6` 生产基线；后续补丁目标为 `.6 -> .7`。

- 个人知识、模型、Git 和 MCP 收拢到个人工作区，并提供跨项目概览。
- 项目概览合并状态与治理，项目配置统一呈现 AI、Git 和 MCP 采用关系；项目规则优先于个人规则。
- Dashboard 增加当前额度，管理后台优化供应商弹窗高度与滚动体验。
- Dashboard 的 390px 填充数据布局补充可收缩网格/面板内容和长字符串断行；browser smoke 在每个视口等待最近任务链接后再执行严格宽度断言，并输出有界溢出诊断。
- Actions 使用精确 `v0.6.0-dev.6` tag，先更新受限发布工具，再完成停写备份、107 条迁移、健康检查和恢复证据输出。
- `v0.6.0-dev.1` 的标签 CI 在覆盖率步骤发现迁移合同断言过期，因此该标签保持失败记录且未部署；后续不移动或复用该标签。
- `v0.6.0-dev.2` 的标签 CI 在独立 PostgreSQL 18 OID10 门禁发现全量账本期望仍为 106；该标签同样保持失败记录且未部署，`.3` 已把当前仓库全量账本期望修正为 107。
- `v0.6.0-dev.3` 的标签 CI 在 OID10 容器前置阶段退出；该标签未部署且保持不变，`.4` 在创建隔离资源前按固定 digest 补拉并重新校验镜像。

## v0.6.0-dev.5 历史失败候选

状态：静态、覆盖率、PostgreSQL 和 OID10 门禁均通过，但填充数据 Dashboard 的 390px browser/accessibility 阶段失败；document scrollWidth 首次运行为 406、重试为 398，均相对 innerWidth 390。该标签从未部署，保持不可移动且不可复用。

## v0.6.0-dev.4 历史失败候选

状态：精确标签 CI 已通过静态、覆盖率、PostgreSQL、OID10 和 Chromium 安装门禁，但在 browser/accessibility 阶段失败；该标签未部署，保持不可移动且不可复用。

- 浏览器门禁发现 Dashboard 额度元数据的对比度不足，修复为可访问的文字颜色后纳入 `.5`。
- Chromium 将半透明滚动条颜色序列化为带 alpha 的十六进制值，而旧断言只接受 `rgba`，`.5` 改为按颜色和 alpha 语义校验。
- 旧的版本硬编码与不精确的“我的模型”定位器暴露了维护问题，`.5` 改为使用统一应用版本和精确定位器。
- 从项目概览打开失败任务时，任务筛选 URL 未同步到页面状态，导致筛选仍显示“全部”；`.5` 补齐 URL 状态同步。
- 浏览器重试复用了固定用户和项目夹具，触发唯一性冲突；`.5` 为重试生成隔离的用户和项目名称。

## v0.5.0-dev.4 发布候选：固定源回滚制品

状态：历史生产基线；在当时的 `.6` 升级前，公网 `/api/health` 返回 `0.5.0-dev.4`，因此它被选为 0.6 生产升级的实际源版本。当前生产基线已为 `v0.6.0-dev.6`。

- 运行中的 `.1` app/worker 先按容器自身 OCI 版本元数据校验；旧容器 image digest 不再要求可被 `docker image inspect` 找到。
- 精确 `.1` source checkout 会在旧 writer 仍运行时重新构建 app/worker，按服务固定源回滚引用并记录可 inspect 的制品 ID；然后才构建 `.4` 目标镜像。
- 目标构建后再次校验源回滚制品没有被覆盖或丢失；只有源制品、目标镜像、备份和数据不变性门禁全部通过才允许停写切换。
- 源 checkout 重建制品不保证与历史丢失镜像字节级相同，隔离恢复演练和生产现场验收仍是必要门禁。

## v0.5.0-dev.3 发布候选：修复保留数据发布维护隔离校验

状态：历史候选，后续由 `v0.5.0-dev.4` 取代；当前生产基线为 `v0.6.0-dev.6`。

- 修复完整 Docker 容器 ID 被 Bash 当作算术表达式解析的问题，维护隔离先校验容器数量，再按字符串校验 PostgreSQL 容器 ID。
- 增加真实 64 位十六进制容器 ID 和额外容器场景的回归测试，避免旧 writer 停止失败时误放行切换。
- `.3` 的发布通道继续复用现有 PostgreSQL、secrets、uploads、用户、平台配置和审计数据；保留 `.2` 的供应商弹窗 Header/Footer 固定与内容区滚动优化。

## v0.5.0-dev.2 历史发布候选：保留数据的供应商弹窗优化

状态：历史候选，后续由 `v0.5.0-dev.4` 取代；当前生产基线为 `v0.6.0-dev.6`。

- 新增供应商弹窗收窄最大高度，Header 与 Footer 固定，只有表单内容区域滚动，避免弹窗占满页面。
- `.2` 的历史发布通道设计为复用现有 PostgreSQL、secrets、uploads、用户、平台配置和审计数据；`.3` 继续使用同一保留数据边界，不使用 `.1` 的 clean reset 流程。
- 生产发布前仍需完成精确提交 CI、备份、迁移、健康、重启持久性和 HTTPS Cookie 验收；本候选说明不替代现场验收。

## v0.4.0-dev.1 受控生产预发布事实

- 平台管理员只负责平台配置、用户运营、安全审计和受限运维，不进入项目、团队或用户工作区；普通用户使用独立的项目工作台。
- 平台供应商、模型连接和逐项能力配置位于 `/admin/models`；每项能力选择已验证可用的模型，测试通过后才能保存并启用。平台额度、连接探测预算和用户运营分别位于 `/admin/credits`、`/admin/operations/probes` 和 `/admin/users`。
- 管理员 Git、旧会员和旧账号矩阵入口已退出正常导航；遗留 URL 只保留服务端守卫后的兼容跳转。
- MCP 只开放个人连接与工具发现、管理员安全审核、项目委托和只读授权控制面；远端调用、审批、派发、结果查看和结果导入仍冻结。
- 真实第三方 OAuth、模型供应商等连接仍需各部署现场验证；不得以历史 v0.3 记录或隔离门禁结果替代。

## 生产与验收边界

- `v0.4.0-dev.1` 的生产发布规则、`v0.5.0-dev.1` 的 clean reset 及 `.2`/`.3` 中间候选只作为历史记录保留；当前应用发布源版本必须由公网健康接口确认。
- 历史 0.6 发布选择实际源 `.4`，通过精确标签 CI、停写备份、106→107 账本迁移、健康和持久性验收；`.6 → .7` 补丁只允许实际源 `.6`，不得用历史 `.4` 或 `.1` 代替。
- 真实第三方模型、Git、OIDC 和 MCP 凭据及远端工作流尚未现场联调。loopback 或隔离门禁不能冒充第三方结果。

GitHub 托管 Runner 的运行状态与本地发布验收分开记录。没有配置真实外部凭据时，相关类别必须明确标记为“未现场验证”，不得冒充第三方结果。

## v0.5.0-dev.1 历史生产基线：移除默认工作区 Owner 初始化

状态：已作为历史生产基线发布；当前 `.6 -> .7` 补丁候选不得改写或复用其 clean reset 语义。

此前 v0.4.x 在创建平台 `admin` 后，仍要求管理员另行创建默认工作区的业务 Owner，才允许普通用户使用 GitHub 登录；OIDC 也依赖这项全局初始化。平台管理账号与业务账号分离是合理的，但全局默认工作区及其人工 Owner 不符合“每位普通用户管理自己的个人工作区”的目标。

v0.5.x 的目标行为：

- 平台 `admin` 初始化后即可独立使用管理台；有效配置 GitHub OAuth 后，普通用户可以发起 GitHub 登录，无须先人工创建业务 Owner。
- 首次通过 GitHub 注册的普通用户获得自己的工作区，并成为该工作区的 Owner；不自动加入全局默认工作区，也不赋予平台 `admin` 工作区或项目权限。
- 用户、工作区、已确认的 Owner 成员关系、账户权益与审计证据必须在受控事务中一致建立；重复回调、并发注册和失败重试不得产生重复账号、工作区或孤立权限。
- 同邮箱已有本地账号时继续禁止静默合并，必须走显式绑定；OAuth 配置错误、回调验证失败和数据库写入失败仍要安全失败关闭。
- 登录页分别说明“平台管理员未初始化”“GitHub OAuth 未配置/无效”等真实原因，不再把缺少人工创建的业务 Owner 统称为“平台初始化尚未完成”。
- 移除默认工作区 Owner 的初始化页面、API 和服务端入口；OIDC 首次建号及管理员创建的本地普通用户也获得自己的个人工作区，加入其他工作区属于额外的成员关系。

当前按“没有普通用户”的空数据状态实施，不设计旧用户、旧会话或项目数据迁移。向前迁移只取消项目默认工作区数据库默认值、移除 `PlatformBootstrap` 的旧 Owner 初始化字段，并清除全新空库中的空默认工作区种子；不会执行用户数据搬迁或会话批量撤销。尚未执行生产迁移。

验收以全新数据库仅完成 `admin` 初始化为起点：GitHub 发起入口应可用；首次成功回调后新用户拥有自己的个人工作区且不自动加入历史默认空间；本地 `admin` 仍只能进入管理台；重复/并发回调、已有邮箱与失败回滚均有聚焦测试。OIDC 与本地新增普通用户也须在同一事务中获得个人工作区，同时保留经授权加入其他工作区的关系；旧 Owner 初始化入口不再可用。真实外部 GitHub/OIDC 授权与回调需在目标环境单独验收，自动化测试不能替代外部联调。
