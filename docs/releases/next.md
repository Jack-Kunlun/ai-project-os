# AI Project OS 未发布候选状态

状态：`READY`

已发布基线：`v5.0.1`

目标版本：`v5.1.0`（未发布）

当前候选已经实现部署安全响应、Worker 健康监控、外部服务现场验收命令、持续集成和最小生产浏览器回归。V5.0.1 的候选门禁、数据库恢复升级、构建、部署和页面验收记录已经归档到 [V5.0.1 发布说明](v5.0.1.md)。

候选源码已统一标记为 `5.1.0`，并已重新通过本地全量自动化、V5.0.1 恢复升级、隔离 Compose、重启持久性和认证浏览器验收。2026-08-31 本次发布范围明确收敛为代码与数据库门禁、按 CI 工作流同序执行的本地流水线、生产 E2E、容器构建/迁移/重启，以及非托管的本地 Compose 发布；托管部署、GitHub 托管 Runner 和真实外部服务现场联调不在本次门禁范围。

真实模型与 Git 已完成最近 24 小时内的连接探测和真实只读工作流；OIDC 与第三方 MCP 尚未配置。这些结果只记录现场状态，不用于证明被排除能力可用，也不以本地替代服务冒充现场验收。未来把任一外部服务纳入部署范围时，仍须按现场手册重新验收。

## 2026-08-31 本地候选验收记录

- Prisma schema 生成与校验通过；50 个迁移目录均包含非空 `migration.sql`。
- Lint、类型检查和 `5.1.0` 生产构建均通过。
- 完整默认测试集共 396 项：361 通过、35 个显式数据库门禁按默认策略跳过、0 失败。
- 统一执行器在隔离 PostgreSQL 18 + pgvector 上实际运行并通过全部 23/23 数据库门禁；每项使用可整体丢弃的专用数据库。
- V5.0.1 备份在精确命名的隔离数据库和卷中恢复：迁移账本由 49 条升级为 50 条且未完成 0 条，144 张既有表的记录数保持一致，新表为空；secrets 与 uploads 的规范化哈希一致。临时数据库、卷和备份目录已精确删除。
- 生产浏览器门禁完成全新数据库迁移、standalone 构建、App/Worker 健康、管理员初始化、Dashboard、使用指南、MCP 连接页与安全响应头检查，1/1 通过且浏览器控制台无错误。
- app、worker、migrate 三张候选镜像构建通过，OCI 版本标签均为 `5.1.0`；隔离 Compose 中 PostgreSQL、app、worker 均为 `healthy`，migrate 以 0 退出，迁移账本为 50 条且未完成 0 条。首次认证 smoke 与数据库重启后的会话/持久性 smoke 均为 1/1 通过，重启后用户、工作区和 Worker 运行记录仍在。
- 一次性候选容器、网络、三个候选卷和三张候选镜像已精确删除；正式 V5.0.1 容器、正式命名卷和独立数据库门禁容器未被重建或删除。
- GitHub Actions 工作流已通过本地 YAML、契约和同序命令验证；`codex/audit-remediation` 候选分支已经推送。远端运行 #1–#3 均在 3–4 秒内失败，平台注解明确说明 Job 未启动，原因是账户近期付款失败或 Actions 支出上限不足；没有执行代码检出、安装或任何测试，因此本记录既不宣称托管 CI 通过，也不把它解释为代码失败。
- 2026-08-31 对正式 V5.0.1 数据库运行只读 `pnpm external:acceptance`：模型与 Git 均为 `ready`，各有最近探测和探测后的成功工作流；OIDC 与 MCP 均为 `missing`，命令按设计返回退出码 2。真实外部服务现场联调已明确排除在本次发布范围之外，未配置能力继续标记为未现场验证。
