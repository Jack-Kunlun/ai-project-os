# 更新日志

本项目采用语义化版本。当前状态为 `CONTROLLED_PRERELEASE`，当前生产运行基线为 `0.6.0-dev.6`，下一受控升级目标为 `0.6.0-dev.7`；尚无正式稳定版基线，首个正式公开版本计划为 `1.0.0`。

V3–V5 条目是内部研发里程碑记录（非正式发布），保留实现、测试、迁移、限制和日期证据，不代表公开发布或生产部署。

## [0.6.0-dev.7] - 无迁移运维状态读取补丁

- 修复备份运维状态读取器对 `pre-deploy-to-v0.6.0-dev.6` 等带 `-dev.N` 的生产备份名称校验过窄的问题。
- 新增从已运行 `.6` 到 `.7` 的 Actions-only 补丁通道；部署前后固定核验 107 条迁移账本、个人知识关系和数据库完整性对象，不执行数据库迁移。
- 更新备份、恢复、root-owned 发布工具、forced-command gateway 和 sudoers 的 `.7` 精确 allowlist；历史 `.5`→`.6` 通道保持可用。

## [0.6.0-dev.6] - Dashboard 响应式与浏览器门禁修复

- 修复填充数据 Dashboard 在 390px 视口下由网格、面板内容和长用户名触发的固有宽度溢出；为可收缩内容补充 `min-w-0`，为面板 Header 启用换行，并为长字符串启用断行。
- 浏览器 smoke 在每个视口导航后等待填充的最近任务链接，再执行严格的文档和 body 宽度断言；失败时输出有界的可见溢出元素标签、类名、文本、矩形和尺寸诊断。
- 统一 package、应用、OCI、生产工作流、迁移预检、备份恢复工具、合同测试和当前文档的候选身份为 `0.6.0-dev.6`。

## [0.6.0-dev.5] - 个人工作区与项目配置重组（历史失败候选）

- 静态、覆盖率、PostgreSQL 和 OID10 门禁均通过，但填充数据 Dashboard 的 390px 浏览器门禁 document scrollWidth 首次运行是 406、重试是 398，均相对 innerWidth 390。
- 该候选从未部署；其标签保持不可移动、不可复用，只作为失败历史保留。

- 新增个人工作区总览和版本化个人知识库；个人模型、Git 与 MCP 连接统一由个人工作区管理，并显示跨项目采用情况。
- 项目状态与治理信息合并到项目概览；新增项目配置页，集中呈现项目采用的 AI、Git 与 MCP 配置及个人规则、项目规则的生效关系。
- Dashboard 显示当前额度；管理后台扩大供应商弹窗内容高度并统一滚动条样式。
- 新增第 107 条迁移和 0.6 专用生产通道：精确校验 0.5 的 106 条迁移账本，停写后创建异地备份，迁移后验证 107 条账本；数据库变更后的失败保持 writer 隔离并输出恢复证据。
- GitHub Actions 在同一运行中从已验证 tag 更新受限发布工具，再执行 0.6 迁移和公网健康检查；服务器仅需一次 bootstrap。
- `v0.6.0-dev.1` 标签因个人知识迁移合同测试仍断言已替换的旧 SQL 表达式而被 CI 拒绝，未进入生产部署；本候选修正该合同并使用新的不可变标签。
- `v0.6.0-dev.2` 标签在独立 PostgreSQL 18 OID10 门禁中发现全量迁移账本期望仍为 106；`.3` 把当前仓库全量账本门禁修正为 107。
- `v0.6.0-dev.3` 标签在 GitHub runner 的 OID10 容器前置阶段退出，未进入生产部署且保持不变；`.4` 在创建测试资源前按同一固定 digest 补拉并重新校验 PostgreSQL 镜像。

## [0.6.0-dev.4] - 浏览器门禁失败的历史候选

- 精确标签 CI 已通过静态、覆盖率、PostgreSQL、OID10 和 Chromium 安装门禁，但 browser/accessibility 阶段失败；该标签未部署，保持不可移动且不可复用。
- 浏览器门禁暴露 Dashboard 额度元数据对比度不足、Chromium CSS 半透明颜色序列化差异、旧版本硬编码和不精确定位器、项目概览任务筛选 URL 未同步，以及重试时固定用户/项目夹具冲突。
- 这些问题已在当前 `0.6.0-dev.6` 候选中修复或纳入稳定的语义断言与隔离夹具策略。

## [0.5.0-dev.4] - 固定保留数据升级的源回滚制品

- 生产部署先从精确的 `.1` 源 checkout 构建 app/worker，并按服务固定可验证的源回滚镜像引用，再构建 `.4` 目标镜像。
- 运行中 `.1` 容器的旧镜像 digest 即使无法由 Docker inspect 找到，也不再阻止源状态校验；旧 digest 只保留作审计证据，不作为回滚前提。
- 切换前复核源回滚引用在目标镜像构建后仍可 inspect，失败则在停止 writer 前失败关闭；回滚使用固定源回滚引用并复核 `.1` 元数据、健康、数据、migration ledger 和 PGDATA 身份。
- 源 checkout 重建的 `.1` 镜像不保证与历史丢失镜像字节级相同，仍需通过隔离恢复演练和生产现场验收。

## [0.5.0-dev.3] - 修复保留数据发布维护隔离校验

- 修复维护隔离阶段将完整 Docker 容器 ID 放入 Bash 算术表达式导致的误判，先校验容器数量，再按字符串校验 PostgreSQL 容器 ID。
- 增加完整 64 位容器 ID 和额外容器污染场景的回归测试，避免停止旧写入者后错误放行。
- 保留 `.2` 的供应商弹窗布局优化及 `.1` 到当前版本的 PostgreSQL、用户、平台配置和审计数据不变性门禁。

## [0.5.0-dev.2] - 保留数据的受控升级候选

- 新增供应商弹窗收窄最大高度，Header 与 Footer 固定，表单内容区域独立滚动，减少页面占用。
- 应用、浏览器健康断言和 OCI 元数据统一升级到 `0.5.0-dev.2`。
- 本版本设计为从已发布的 `v0.5.0-dev.1` 保留数据升级；不删除 PostgreSQL、secrets、uploads、用户、平台配置或审计数据。
- 当前仍是受控预发布候选，待精确标签 CI、升级门禁和双环境现场验收完成后才能宣称生产发布。

## [0.5.0-dev.1] - 历史生产发布基线

- 移除默认工作区 Owner 的二次初始化。平台管理员初始化后独立进入管理台；GitHub OAuth、OIDC 和本地新增普通用户各自创建个人工作区及已确认的 Owner 成员关系。
- GitHub 登录不再受全局业务 Owner 门禁阻断；同邮箱本地账号仍须显式绑定，失败回调保持事务回滚。
- 新增第 106 条迁移，取消项目默认工作区并移除过时的 Owner bootstrap 字段；仅清理没有业务引用的空旧种子工作区。
- 历史生产目标曾锁定精确标签 `v0.5.0-dev.1`，要求对应提交的 CI、异地备份与服务器新版部署工具；该版本的 clean reset 语义不适用于 `.2` 升级。

## [0.4.0-dev.1] - 受控生产预发布

本节记录 v0.4.0-dev.1 管理台可用性基线和当前生产发布边界。它仍是 prerelease，不是稳定版或 GitHub Latest。

### 已实现事实

- 管理工作台使用 Dashboard、用户与权益、配置中心、安全中心和运维中心五个一级入口；Header、侧栏固定，正文区域独立滚动。
- `/admin/models` 同时承载供应商连接、模型信息和逐项能力配置。每项能力可自由选择已验证可用的模型；填写连接后可以先测试，只有测试通过才能保存并启用，生命周期文案统一为“启用/停用”。
- Dashboard 聚合今日用户增长、平台调用/Token 消耗和额度状态；缺少可靠证据时显示未取得，不以零值代替。
- GitHub OAuth、模型供应商和真实生产部署仍需在目标环境独立现场验收；本地测试或 loopback 证据不能替代第三方结果。

### 发布边界

- `v0.4.0-dev.1` 是当前唯一批准的受控生产预发布；旧 `v0.3.0-dev.1`、`v0.2.0-dev.1` 和其他 dev tag 均不得进入生产标签通道。
- 生产 clean reset 不迁移 v0.3 数据库；备份恢复、精确提交 CI、全新数据库初始化和生产现场验收仍须在发布执行阶段确认。
- 生产部署使用一次性的 clean reset：强制完成加密异地备份后只删除 PostgreSQL 数据卷，v0.3 用户、配置、额度、审计和供应商数据不带入新库，管理员必须从 `/setup` 重新初始化；卷删除后不提供自动回滚。

## [0.3.0-dev.1] - 受控生产预发布

本节记录 v0.3.0-dev.1 受控生产预发布已经落地的产品边界和界面方向。它仍是 prerelease，不是稳定版或 GitHub Latest。

### 开发线已实现事实

- 平台管理员与普通用户使用完全分开的工作台。平台管理员只负责平台配置、用户运营、安全审计和受限运维，不进入项目、团队或用户工作区。
- v0.3 历史版本曾将平台模型与默认路由分开维护；当前 v0.4 已将供应商、模型和逐项能力配置收拢到 `/admin/models`，额度、探测预算和用户运营仍分别位于 `/admin/credits`、`/admin/operations/probes` 和 `/admin/users`。
- 管理员 Git 连接、旧会员入口和账号矩阵入口已退出正常导航；遗留 URL 只保留服务端守卫后的兼容跳转。
- 平台模型页面聚焦供应商与模型连接；倍率在默认路由页面展示，额度记录和探测预算分别在独立页面维护。
- MCP 只开放个人连接与工具发现、管理员安全审核、项目委托和只读授权控制面；远端调用、审批、派发、结果查看和结果导入仍冻结。

### 发布边界

- v0.3.0-dev.1 发布当时是唯一批准的受控生产预发布；当前生产目标已更新为 v0.4.0-dev.1，旧 v0.3.0-dev.1、v0.2.0-dev.1 和其他 dev tag 均不得进入生产标签通道。
- 真实第三方模型、Git、OIDC 和 MCP 凭据及远端工作流仍需在目标环境独立现场验收。

## [0.2.0-dev.1] - 改造开发线

当前版本记录 0.2.x 产品改造开发线中已经实现的事实，并作为精确标签 `v0.2.0-dev.1` 的受控生产预发布候选；它不是稳定版或 RC，也不会成为 GitHub Latest。

### 开发线已实现事实（不构成正式发布资格）

- 平台默认托管模型由系统管理员在 `/admin/models` 维护并按平台额度提供；有效会员可在个人中心维护个人模型，但必须经连接所有者与项目 Owner 双确认委托后才能按项目使用，个人模型不会自动替代平台默认模型。
- Git 连接归属具体用户；项目页支持双确认后的一次性手动只读读取，自动化、写入/提交、旧版项目/管理员连接器和旧 PAT 路径保持关闭。
- MCP 个人连接与工具发现、管理员净化快照审核、项目连接委托和只读工具授权控制面已开放；远端动作调用、调用审批、派发、结果查看/导入和公开外发入口仍冻结，不能通过内部 API 绕过。
- `/system/account-access` 提供系统管理员账号状态治理和只读有效访问矩阵；停用账号仍可审计但有效结果为 `effective=false`，system admin 不隐含工作区或项目 Owner 权限。
- 当前数据库迁移数为 102；生产升级在 clean-slate 破坏性 DDL 前安装数据库写入栅栏，阻止预检与迁移之间出现新的遗留成员、工作区模型或旧项目路由数据。

### 未完成与未现场验证

- R-05 本地全量发布门禁已完成；真实第三方模型、Git、OIDC 和 MCP 凭据及远端工作流尚未现场联调。
- 本开发线的本地、loopback 或隔离门禁证据不能替代真实供应商、Git 服务、OIDC 身份源或 MCP 服务的现场验收。

## [0.1.0-dev.1] - 内部开发基线

当前版本用于内部开发、集成和验收。生产部署入口在首个正式 `v1.0.0` 前静态禁用，内部开发版本不得进入生产 tag 通道。

预发布说明：[docs/releases/v0.1.0-dev.1.md](docs/releases/v0.1.0-dev.1.md)

### 基线能力

- 固定项目资料、事实审核、记忆索引、计划、状态、动作审批、自动化和管理员工作台等当前能力。
- 固定工作区/项目 RBAC、加密外部凭据、模型调用审计、平台额度及 54 个 Prisma 迁移的当前实现。

### 已知限制

- 用户等级、个人模型、Git/MCP 用户所有权和平台默认模型路由将在 0.2.x 改造，本版本不宣称已具备目标权益模型。
- 真实模型、Git、OIDC 和第三方 MCP 仍需使用目标环境凭据独立验收。

## [5.1.2 内部研发里程碑（非正式发布）] - 2026-08-31

里程碑记录：[docs/releases/v5.1.2.md](docs/releases/v5.1.2.md)

### 安全

- 生产 Compose 显式把 `AI_PROJECT_OS_SECURE_COOKIES` 传递给应用容器，确保 HTTPS 部署启用的安全 Cookie 配置不会停留在宿主机 `.env`。
- 增加 Compose 回归断言，固定该变量只注入 `app` 服务，并保留本地 HTTP 所需的安全默认值。

### 验证

- 精确候选提交 `ec4f7e3` 通过 GitHub Actions CI #13：Prisma/迁移、Lint、类型检查、覆盖率、23 个 PostgreSQL 门禁、生产浏览器/WCAG、性能预算和隔离 Compose 候选门禁全部成功。
- 同一候选通过本地 `releaseEligible:true` 容器门禁，确认版本 `5.1.2`、50 个迁移、健康、重启持久性和精确资源清理。

## [5.1.1 内部研发里程碑（非正式发布）] - 2026-08-31

里程碑记录：[docs/releases/v5.1.1.md](docs/releases/v5.1.1.md)

### 验证

- 增加覆盖全部 `src` TypeScript/TSX 的测试覆盖率门禁，当前基线要求行、分支和函数覆盖率分别不少于 60%、78% 和 67%。
- 生产浏览器门禁增加 setup、Dashboard、项目列表、使用指南和 MCP 连接页的 WCAG 2.2 A/AA 自动扫描。
- 增加生产客户端 gzip 体积预算，持续约束共享脚本、单文件、全局样式、全部静态资源和五个关键路由。

### 部署

- 增加隔离本地 Compose 候选门禁，自动验证镜像版本、迁移、健康与重启持久性，并精确清理候选容器、网络、卷和镜像。

## [5.1.0 内部研发里程碑（非正式发布）] - 2026-08-31

里程碑记录：[docs/releases/v5.1.0.md](docs/releases/v5.1.0.md)

### 新增

- 增加 Worker 持久化心跳、容器健康检查和 `/api/health` 独立 Worker 状态，便于区分应用存活与后台执行能力。
- 增加真实模型、Git、OIDC 和第三方 MCP 的只读现场验收命令，以及 PostgreSQL 与生产浏览器发布门禁的 GitHub Actions 工作流。

### 安全

- 为所有页面和 API 增加 CSP、点击劫持、MIME 嗅探、Referrer 与浏览器能力限制响应头，并提供 HTTPS 反向代理与入口限流基线。
- Worker 运行日志改为稳定结构化事件，不输出凭据、外部正文或内部异常细节。

### 部署

- Compose 支持覆盖端口和三个持久化卷名，可在不触碰正式实例的前提下运行隔离候选部署与浏览器验收。

## [5.0.1 内部研发里程碑（非正式发布）] - 2026-08-31

里程碑记录：[docs/releases/v5.0.1.md](docs/releases/v5.0.1.md)

### 安全

- 来源正文与 SHA-256 在数据库写入时强制一致，来源证明除生命周期标记外不可改写。
- MCP 只读工具必须由管理员对精确工具定义、网络与凭据指纹追加式认证；撤销或任一指纹漂移都会失败关闭。
- 收紧 OIDC 回跳、匿名登录流程容量、JSON/multipart 请求体和 PDF 视觉渲染资源边界，并升级 `yauzl` 安全修复版本。

### 验证

- 修复 ProjectSource provenance PostgreSQL gate 与 integrity smoke 对第 49 个迁移的兼容/一致性验证。
- 增加统一的一次性 PostgreSQL 门禁执行器，完整覆盖 22 个显式数据库 gate，并限制为固定回环测试端口和白名单数据库。

## [5.0.0 内部研发里程碑（非正式发布）] - 2026-08-30

里程碑记录：[docs/releases/v5.0.0.md](docs/releases/v5.0.0.md)

### 新增

- 增加时态项目世界模型，以当前有效事实、人工关系、冲突和计划健康度确定性计算项目状态。
- 增加独立“项目状态”页面，支持七类版本绑定事实关系、关系退役、同类型事实替代链、不可变状态快照和治理审计。
- Dashboard 增加跨项目状态提醒；项目简报与只读智能体复用同一状态、当前事实和输入指纹。

### 安全

- 关系端点固定同项目最新修订；事实变化后旧关系标记为陈旧，不会静默跟随。
- 替代链由服务端和数据库共同拒绝跨类型、重复后继、分叉、并发覆盖和循环。
- 被替代事实继续保留确认时间、来源证据和 AI 候选来源一致性；关系、快照和审计采用追加式保护。
- AI 不会自动确认事实、建立关系、替代事实或覆盖确定性状态，也不会获得代码、Git、Shell、MCP 写入或部署能力。

## [4.1.0 内部研发里程碑（非正式发布）] - 2026-08-30

里程碑记录：[docs/releases/v4.1.0.md](docs/releases/v4.1.0.md)

### 新增

- 项目工作项增加可编辑负责人、独立验收标准与证据关联；进行中要求负责人和验收标准，完成还要求活动证据。
- 计划页增加确定性项目健康，覆盖逾期、受阻、未完成依赖、负责人/验收/证据缺口、证据过期、待采纳建议、仓库变化和动作审批。
- 成功且完成对账的仓库同步可生成待评估变化信号，由 Editor 或 Owner 人工关联到工作项或明确忽略。
- 自动化增加“项目计划健康提醒”，可配置到期窗口与负责人提醒；Dashboard 增加跨项目风险与运营提醒。

### 安全

- 负责人只允许当前项目 Owner/Editor 或工作区 Owner/Admin；权限撤销后不能继续启动或完成工作项。
- 工作项证据固定规范化快照和 SHA-256 指纹，只允许软移除；完成与取消后的工作项不可改写。
- 仓库变化信号只陈述可验证变化，不推断业务影响，不自动生成工作项、动作或代码变更。
- 健康检查只读取本地数据库，不调用模型或外发项目内容；发送通知前重新核对接收者当前访问权。

### 验证

- 空数据库完整应用 47 个迁移；V4.0 备份完成隔离恢复与 V4.1 升级，关键业务表计数一致。
- 完整测试 365 项：333 通过、32 项需显式数据库门禁而跳过、0 失败；Action Engine、MCP、项目计划、项目智能体和 V3 五组真实 PostgreSQL 门禁另行通过。
- Prisma 生成与校验、类型检查、Lint 和本地生产构建通过。
- 已登录生产预览完成 Dashboard、项目计划、自动化和使用指南验收；计划按钮保持文本居中，控制台无警告或错误。
- Docker Compose 正式升级成功：迁移容器正常退出，应用与 PostgreSQL 健康，Worker 持续运行；重启应用和 Worker 后健康接口、版本与既有数据保持正常。

## [4.0.0 内部研发里程碑（非正式发布）] - 2026-08-30

里程碑记录：[docs/releases/v4.0.0.md](docs/releases/v4.0.0.md)

### 新增

- 成功的 MCP 只读动作结果可由 Editor 或 Owner 人工固化为未审核项目资料；导入绑定动作、输入、结果、内容和来源指纹，并保持幂等。
- 增加独立“项目计划”页面，支持目标、工作项、优先级、状态、目标归属、依赖和追加式审计。
- 只读智能体建议可由用户纳入为带完整引用快照、输入清单指纹和证据指纹的 `proposed` 工作项。
- 项目受限 JSON 导出升级到 Schema V3，包含结果导入与项目计划安全字段。

### 安全

- MCP 结果纳入不会自动确认事实、重建索引、调用模型或触发智能体；数据库保护导入记录不可修改或删除。
- 智能体建议必须由用户纳入并再次接受后才能进入已计划；来源与证据不可改写。
- 工作项依赖拒绝自依赖、重复活动依赖和循环依赖；移除只做软移除，计划审计不可变。
- 项目计划不会自动创建动作，也不开放 MCP 写入、Shell、代码写入、Git 写入、合并或部署能力。

### 验证

- 从空数据库完成全部 46 个迁移；V3.2 备份完成隔离恢复与第 45、46 个迁移升级，关键数据计数一致，5 张 V4 表和 5 个保护触发器核对通过。
- 完整测试 360 项：328 通过、32 项需显式数据库门禁而跳过、0 失败；Action Engine、MCP、项目计划、智能体建议和 V3 五组真实 PostgreSQL 门禁另行通过。
- Prisma Schema、类型检查、Lint、本地与 Docker 生产构建、Docker Compose 运行验收通过。
- 认证后页面确认 V4.0.0、项目计划、MCP 结果纳入和页面指南；共享 Header 一致，关键按钮文字居中，浏览器无警告或错误。

## [3.2.0 内部研发里程碑（非正式发布）] - 2026-08-30

里程碑记录：[docs/releases/v3.2.0.md](docs/releases/v3.2.0.md)

### 新增

- 增加页面化远程 MCP Streamable HTTP 连接，支持无认证或加密 Bearer Token、显式内网授权、DNS 固定和工具目录发现。
- 增加追加式工具定义快照；只有明确声明 `readOnlyHint=true` 且 `destructiveHint=false` 的定义可以进入项目授权。
- 增加项目“工具权限”页面；项目 Owner 逐项授权，Editor 或 Owner 填写受 Schema 约束的参数并创建调用动作。
- Action Engine 增加 MCP 只读调用能力；每次调用必须由项目 Owner 单独审批，并在动作记录中保存受限文本、结构化结果和结果指纹。

### 安全

- 服务端 annotations 只作为未受信声明；管理员仍需信任远端服务，并使用最小权限只读凭据。
- 动作指纹绑定项目、授权、工具定义、参数、DNS 和凭据；连接停用、定义更新、地址变化、凭据轮换或撤权均失败关闭。
- 数据库禁止 MCP 自动策略，强制 MCP 动作保存为逐次审批，并保护工具定义快照与授权审计不可变。
- 当前不提供 stdio、本地子进程、旧式 HTTP+SSE、执行中交互输入、MCP 写操作、模型自动调用、Shell、代码写入、Git 写入或部署权限。
- MCP 结果不会自动进入项目记忆、RAG 索引或模型上下文，图片、音频和资源链接不会自动展开。

### 验证

- 从空数据库完整应用 44 个迁移；V3.1 备份完成隔离恢复并应用第 44 个迁移，实际部署迁移账本、4 张 MCP 表和 3 个保护触发器核对通过。
- 完整测试 349 项：318 通过、31 项需显式数据库门禁而跳过、0 失败；V3.2 MCP、原有 Action Engine 和 V3 三组真实 PostgreSQL 门禁另行通过。
- Prisma Schema、类型检查、Lint、生产构建和 Docker Compose 运行验收通过。
- 认证后页面确认 V3.2.0、MCP 连接、项目工具权限、动作中心与页面指南；共享 Header 一致，关键按钮文字居中，浏览器无警告或错误。

## [3.1.0 内部研发里程碑（非正式发布）] - 2026-08-29

里程碑记录：[docs/releases/v3.1.0.md](docs/releases/v3.1.0.md)

### 新增

- 增加项目“动作与审批”页面，集中配置能力策略、创建动作、执行审批、取消未执行动作并查看状态账本。
- 增加仓库同步、网页刷新和记忆质量检查三项内置动作；仓库与网页外部只读动作默认逐次审批，本地质量检查默认自动排队。
- 增加输入指纹、策略快照、24 小时审批窗口、持久化 Worker 租约、完成/失败通知和不可变动作审计。

### 安全

- 项目 Owner 才能修改动作策略或审批；Editor 可创建动作，Viewer 保持只读。
- 数据库固定动作能力白名单、合法状态迁移、审批指纹绑定和不可变审批/审计/策略修订。
- 运行租约过期时失败关闭，不自动重复外部读取；归档会取消未执行动作并阻止新动作领取。
- 通知持久化故障不会反向改写已完成动作的终态；同一幂等键不能静默复用于不同动作。

### 验证

- 空库完整应用 43 个迁移，升级前数据库备份完成隔离恢复及 42 到 43 个迁移的升级演练。
- 完整测试、真实 PostgreSQL Action Engine 门禁、类型检查、Lint、生产构建和 Docker Compose 运行验收通过。
- 认证后页面确认 V3.1.0、动作中心、共享 Header、页面指南和按钮文字居中，无浏览器警告或错误。

## [3.0.0 内部研发里程碑（非正式发布）] - 2026-08-29

里程碑记录：[docs/releases/v3.0.0.md](docs/releases/v3.0.0.md)

### 新增

- 支持文件、图片、网页、本地文件夹和多个 Git 服务仓库作为项目记忆来源。
- 支持页面配置 OpenAI、DeepSeek、Qwen、GLM，以及项目级视觉、抽取、向量和生成模型路由。
- 支持自动抽取、人工审核、统一向量索引、语义搜索、引用式 RAG、项目简报和受约束的只读智能体。
- 支持持久化自动化 Worker、通知中心、记忆质量与生命周期治理。
- 支持工作区成员、项目成员、RBAC、邀请链接和 OpenID Connect 登录。

### 变更

- Dashboard 专注跨项目概览，项目管理、模型设置、连接器、团队和个人中心使用同级导航入口。
- Git 连接从 GitHub 扩展到 Gitee、GitLab、自建 GitLab、Gitea、Forgejo 和通用 Git。
- Docker Compose 增加独立迁移任务、自动化 Worker、上传卷和凭据主密钥卷。

### 安全

- 模型、Git 与 OIDC Secret 使用 AES-256-GCM 加密，主密钥保存在数据库之外。
- Git、网页和 OIDC 网络访问增加 DNS 固定、云元数据阻断、内网显式授权和响应限制。
- OIDC 使用 Authorization Code + PKCE，并校验 issuer、audience、nonce、有效期和允许的签名算法。
- 工作区与项目权限在服务端统一执行；AI 候选仍需人工确认后才能成为项目事实。

### 验证

- Prisma 生成与校验、完整迁移链、类型检查、Lint、完整测试和生产构建通过。
- 真实 PostgreSQL V3 集成、Docker Compose 四服务状态和认证后页面 smoke 通过。
- PostgreSQL 自定义格式备份完成隔离恢复及 34 到 42 个迁移的升级演练，原项目与工作区归属保持完整。
