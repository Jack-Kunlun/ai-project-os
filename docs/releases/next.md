# AI Project OS 未发布候选状态

状态：`BLOCKED`

已发布基线：`v5.0.1`

目标版本：`v5.1.0`（未发布）

当前候选已经实现部署安全响应、Worker 健康监控、外部服务现场验收命令、持续集成和最小生产浏览器回归。V5.0.1 的候选门禁、数据库恢复升级、构建、部署和页面验收记录已经归档到 [V5.0.1 发布说明](v5.0.1.md)。

候选源码已统一标记为 `5.1.0`。此前在 `5.0.1` 版本标识下完成的本地自动化证据证明了新增代码路径，但不替代对最终 `5.1.0` 候选的重新验收。托管 GitHub Actions 尚未在远端执行，真实外部服务现场门禁也未满足，因此候选仍不得发布或部署到正式实例。只有源码、健康接口、绿色 CI 或容器启动均不能单独构成发布依据。

真实外部模型、Git、OIDC 和第三方 MCP 服务已有只读验收门禁，但当前部署没有现场凭据与成功工作流证据，状态仍为“未现场验证”。不得用本地替代服务结果冒充；只有按现场手册操作后 `pnpm external:acceptance` 返回 `ok: true`，此门禁才可关闭。

## 2026-08-31 本地候选验收记录

- Prisma schema 生成与校验通过；50 个迁移目录均包含非空 `migration.sql`。
- 完整默认测试集共 396 项：361 通过、35 个显式数据库门禁按默认策略跳过、0 失败。
- 统一执行器在隔离 PostgreSQL 18 + pgvector 上实际运行并通过全部 23/23 数据库门禁；每项使用可整体丢弃的专用数据库。
- 生产浏览器门禁完成全新数据库迁移、standalone 构建、App/Worker 健康、管理员初始化、Dashboard、使用指南、MCP 连接页与安全响应头检查，1/1 通过且浏览器控制台无错误。
- app、worker、migrate 三张候选镜像构建通过，OCI 版本标签均为 `5.0.1`；隔离 Compose 中 PostgreSQL、app、worker 均为 `healthy`，migrate 以 0 退出，迁移账本为 50 条且未完成 0 条。随后对该容器应用再次执行认证浏览器 smoke，1/1 通过。
- 一次性候选容器、网络、三个候选卷和三张候选镜像已精确删除；正式 V5.0.1 容器、正式命名卷和独立数据库门禁容器未被重建或删除。
- GitHub Actions 工作流已通过本地 YAML、契约和等价命令验证，但当前分支未推送，因此不能宣称托管 CI 已通过。
- 2026-08-31 对正式 V5.0.1 数据库重新运行只读 `pnpm external:acceptance` 返回退出码 2：模型连接已配置且最近探测成功，但缺少发生在探测之后的真实模型工作流；Git、OIDC、MCP 均未配置。此项仍是候选发布阻塞门禁。
