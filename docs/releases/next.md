# AI Project OS 内部开发状态

状态：`INTERNAL_DEVELOPMENT`

正式发布基线：无

当前 GitHub 预发布基线：`v0.1.0-dev.1`（内部开发、非 Latest、非生产）

下一开发版本：`0.2.0-dev.1`

首个正式公开版本：计划 `1.0.0`

当前不记录正式发布候选。`v0.1.0-dev.1` 仅用于固定 0.2.x 改造前的可追溯 GitHub prerelease 基线；V3–V5 的安全、功能和验收材料均为内部研发里程碑记录（非正式发布），分别保留在本目录中。内部开发版本的容器门禁不构成生产发布依据。

后续内部开发变更必须记录范围、验收证据和风险；首个正式 `v1.0.0` 发布前，生产部署入口保持静态禁用。生产部署还需单独核验环境变量、数据库备份、迁移、健康、重启持久性与 HTTPS Cookie。只有源码、健康接口或容器启动均不能单独构成正式发布依据。

## 当前开发线证据

- `0.2.0-dev.1` 当前仍是 `INTERNAL_DEVELOPMENT`，没有正式发布候选，也不是 RC。
- 当前数据库迁移数为 `101`。
- 代码库外任务台账当前为 `50 CLOSED / 0 PARTIAL / 0 OPEN / 1 EXCLUDED`；该数字不替代本文件列出的发布门禁。
- `ADM-008`（统一有效访问矩阵）已关闭：`/system/account-access` 为正式系统管理员入口；矩阵是只读审计，停用账号的结果为 `effective=false`，system admin 不隐含工作区或项目 Owner 权限。
- `R-04` 文档一致性与 `R-05` 全量发布门禁均已完成；当前开发线的本地工程候选门禁已通过，但这不构成正式发布候选或 RC。
- `R-05` 已通过静态门禁、61 项 PostgreSQL 门禁、生产浏览器门禁、性能门禁，以及本地工程候选和正式本地 Compose 环境的健康、迁移、业务数据重启持久性与 Worker 重启验收；当前迁移数为 `101`。
- 真实第三方模型、Git、OIDC 和 MCP 凭据及远端工作流尚未现场联调。MCP 个人连接/工具发现、管理员净化快照审核、项目连接委托和只读工具授权控制面已开放，但远端动作调用、调用审批、派发、结果查看/导入仍冻结；loopback 或隔离门禁不能冒充第三方结果。

GitHub 托管 Runner 的运行状态与本地发布验收分开记录。没有配置真实外部凭据时，相关类别必须明确标记为“未现场验证”，不得冒充第三方结果。
